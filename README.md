# 本地笔记与待办

一个在本地浏览器中运行的个人笔记与待办工具，包含“笔记、待办、我的”三个模块。

## 启动

```text
node server.mjs
```

默认地址：<http://127.0.0.1:4173>

## 测试

```text
node --test "tests/*.test.mjs"
```

测试包含核心领域单元测试与浏览器端到端测试。浏览器测试默认使用本机 Chrome 或 Edge，也可以使用 `BROWSER_PATH` 指定浏览器路径。

## 数据

应用只把数据保存在当前浏览器的本地存储中，不发送任何网络请求，也不需要登录或后端服务。
