# 个人笔记与待办软件 PRD

## 1. 产品目标和使用场景

### 产品目标

做一款运行在个人电脑浏览器中的本地笔记与待办工具，帮助用户完成三件事：

1. 按分类记录并回顾自己的感悟、生活和思考。
2. 创建有时限的待办，完成状态清晰，过期未完成的待办自动清理。
3. 在一个统一入口查看本月和全部内容，了解自己的创作数量。

### 使用场景

- 个人在本地电脑上使用，不登录、不联网、不同步。
- 打开浏览器即可使用，关闭后数据仍保留在这台电脑上。
- 数据只属于本机用户，不需要考虑多用户、权限或多设备。

### 核心边界

- 第一版是单用户本地应用，数据保存在浏览器本地。
- 不依赖后端服务或云数据库。
- 第一版以桌面浏览器为主要使用环境。

## 2. 整体页面框架和导航结构

### 应用外壳

应用采用单页应用结构，固定部分组成如下：

- 左侧固定一级导航：笔记、待办、我的。
- 顶部为当前模块的标题栏和主要操作区。
- 右侧为当前模块的内容区。
- 底部或页面角落提供统一的操作反馈，如“已保存”“已删除”。

### 页面结构

```text
个人笔记与待办软件
├── 笔记
│   ├── 全部
│   ├── 感悟
│   ├── 生活
│   └── 思考
├── 待办
└── 我的
    ├── 我的创作
    │   ├── 本月创作
    │   └── 全部创作
    └── 设置
```

### 导航规则

- “笔记”“待办”“我的”是三个一级模块，任何时候都可以从左侧导航切换。
- “我的”页面内部用两个二级页签承载“我的创作”和“设置”。
- “我的创作”内部再用两个筛选项承载“本月创作”和“全部创作”。
- 笔记页的“全部 / 感悟 / 生活 / 思考”用于筛选查看；其中“全部”不是可保存的分类。
- 新建笔记时只能从“感悟、生活、思考”中选择一个真实分类。
- 当前所处模块、分类或筛选状态应尽量记录在页面状态或地址参数中，刷新后尽量回到相同位置。

## 3. 每个模块的用途

### 3.1 笔记

用途：记录带分类的个人内容，并支持集中查看和回顾。

分类固定为：

- 感悟
- 生活
- 思考

查看入口：

- 全部：同时展示三个分类的笔记。
- 感悟 / 生活 / 思考：只展示对应分类。

### 3.2 待办

用途：记录有时间要求的待办事项，并管理它们的完成状态。

规则：

- 待办需要手动标记完成。
- 时间已过期且仍未完成的待办会被自动删除。
- 已完成的待办不会被过期规则删除。
- 自动清理只会在应用打开或运行期间触发；应用关闭期间不执行，下次打开时再按当前时间清理。

### 3.3 我的

用途：统一展示本地创作情况和为后续设置内容预留入口。

#### 我的创作

展示两个范围：

- 本月创作：只统计当前自然月内的内容。
- 全部创作：统计当前仍存在、未被删除的全部内容。

展示内容包括：

- 内容总数。
- 笔记篇数。
- 待办条数。
- 按日期组织的内容列表，笔记和待办可以混排。

#### 设置

第一版只保留入口和占位页面，不放置真实设置项。设置内容在后续版本确定后再加入。

## 4. 每个模块第一版最必要的数据和操作

### 4.1 笔记

笔记第一版需要的数据：

- 唯一标识。
- 标题。
- 正文内容。
- 分类：感悟、生活、思考。
- 手动填写的日期，用于笔记排序和“我的创作”统计。
- 自动记录的创建或更新时间，用于技术排查和后续功能。

笔记第一版需要的操作：

- 按“全部 / 感悟 / 生活 / 思考”查看笔记。
- 新建笔记。
- 编辑笔记，包括修改标题、内容、分类和日期。
- 删除笔记。
- 保存所有修改到本地。

输入约束：

- 新建或保存时，标题和正文不能为空。
- 新建时分类不能为空。
- 日期由用户手动填写，不允许留空。

### 4.2 待办

待办第一版需要的数据：

- 唯一标识。
- 待办内容。
- 用户填写的日期和时间。
- 完成状态。
- 自动记录的创建时间。

待办第一版需要的操作：

- 新建待办。
- 编辑待办内容或时间。
- 手动标记完成或取消完成。
- 手动删除待办。
- 自动删除过期且未完成的待办。

输入约束：

- 待办内容不能为空。
- 日期和时间由用户手动填写，不允许留空。
- 手动删除需要二次确认。
- 自动删除需要明确反馈，避免用户误以为数据丢失。

### 4.3 我的创作

“我的创作”本身不保存独立数据，数据由笔记和待办实时计算得出。

需要的数据来源：

- 当前月份范围下的笔记数量。
- 当前月份范围下的待办数量。
- 当前范围下的内容列表。
- 全部范围下的总内容数、笔记数、待办数。

需要的操作：

- 在“本月创作”和“全部创作”之间切换。
- 查看按日期排列的创作列表。
- 内容发生新建、编辑、删除、完成或自动清理后，统计立即更新。

### 4.4 数据存储

- 第一版数据保存在浏览器本地存储中，不向外部发送。
- 新建、编辑、删除、完成状态变化后应立即保存。
- 刷新或重新打开页面后数据仍然存在。
- 数据被删除后不提供恢复或回收站。

## 5. 第一版必须完成的功能

1. 单页应用外壳：提供“笔记、待办、我的”三个一级导航。
2. 本地持久化：所有笔记、待办和状态变化刷新后不丢失。
3. 笔记功能：
   - 新建笔记。
   - 编辑笔记。
   - 删除笔记。
   - 按“全部 / 感悟 / 生活 / 思考”查看。
4. 待办功能：
   - 新建待办。
   - 编辑待办。
   - 手动标记完成。
   - 手动删除。
   - 过期未完成的自动删除。
5. 我的创作功能：
   - “本月创作”和“全部创作”两个范围。
   - 总内容数、笔记数、待办数。
   - 按日期展示创作列表。
6. 设置占位页：保留“设置”入口和空页面。
7. 桌面浏览器适配：布局清晰，按钮、文字、列表在常见桌面窗口宽度下不重叠。

## 6. 第一版暂时不做的功能

1. 账号登录、云同步、跨设备使用。
2. 后端服务和网络请求。
3. 系统通知、弹窗提醒、到期提醒。
4. 数据导出、导入和自动备份。
5. 回收站和撤销删除。
6. 自定义分类。
7. 全文搜索、高级排序和复杂筛选。
8. 重复待办、周期待办。
9. 手机端专项适配。
10. 真实设置功能。

## 7. 可以实际检查的验收标准

以下验收标准可以直接在浏览器中检查，也可以转换为自动化测试。

### 整体导航

- 首次打开后可以看到“笔记、待办、我的”三个入口，三个入口都能正常切换。
- 进入任一模块后，页面能展示当前模块的标题和内容。
- 在“我的”页面可以看到“我的创作”和“设置”，并能来回切换。

### 数据持久化

- 新建一条笔记或待办后刷新页面，内容仍然存在。
- 修改一条笔记或待办后刷新页面，修改后的内容仍然存在。
- 删除一条笔记或待办后刷新页面，被删除内容不再出现。
- 完成或取消完成一条待办后刷新页面，状态保持不变。
- 浏览器开发者工具中看不到外部网络请求。

### 笔记验收

- 在“全部”下新建一条笔记，刷新后可以在“全部”中看到。
- 新建时选择“感悟”，该笔记只出现在“感悟”和“全部”，不出现在“生活”和“思考”。
- 标题或正文为空时无法保存。
- 编辑标题、内容、分类、日期后保存，列表中的内容同步更新。
- 删除笔记前出现确认框；取消时笔记仍在，确认后笔记从所有查看入口消失。
- 笔记列表按用户填写的日期排序，日期较新的笔记排在前。

### 待办验收

- 待办内容为空或时间为空时无法添加。
- 新建待办后出现在待办列表中，刷新后仍在。
- 点击完成标记后，待办进入已完成状态，再次点击可以取消完成。
- 编辑待办内容和时间后，列表显示更新后的内容。
- 手动删除待办前出现确认框，确认后待办消失。
- 将一个未完成待办的时间设置为已经过期，应用运行检查后该待办被自动删除。
- 将一个已完成待办的时间设置为已经过期，该待办不会自动删除。
- 自动清理发生时页面给出可感知的反馈。

### 我的创作验收

- 在“本月创作”和“全部创作”间切换时，数字和列表范围同步变化。
- 新建一篇笔记后，“全部创作”和“本月创作”中的笔记数都增加。
- 本月范围只展示日期落在当前自然月的内容。
- 全部范围展示所有未被删除的内容。
- 删除笔记或待办后，对应数量减少，列表移除该内容。
- 待办过期被自动清理后，不再计入“全部创作”。
- 已完成待办仍然计入待办条数。

### 设置占位页

- “设置”页可以正常打开并展示第一版占位内容。
- 设置页不包含任何虚假或不可用的设置开关。
